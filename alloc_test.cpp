#include <gtest/gtest.h>
#include "log_alloc.h"
#include "dumb_container.h"

TEST(AllocTest, Test1)
{
// test if container with logging_allocator works 
}

TEST(AllocTest, Test2)
{
// test if memory is freed after container is destroyed
}
